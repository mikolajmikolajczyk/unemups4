---
id: TASK-87
title: >-
  gcn: decoder hardening — MUBUF literal soffset, Inst carries
  K/omod/offset/raw, opcode verify vs llvm-mc, golden cross-check
status: Done
assignee: []
created_date: '2026-07-12 10:22'
updated_date: '2026-07-12 10:39'
labels:
  - gpu
  - gcn
dependencies: []
priority: high
ordinal: 86000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Code-review round-5 of task-38 (GCN decoder). Finder A verified all bit-field extraction + slice-safety + PC-clamp CORRECT for every class; the corpus-exercised decode is solid (golden spot-check passthrough_vs.dis matches .s + finder D confirmed exercised operand/waitcnt/export ranges). All fixes here are UNEXERCISED-path hardening + Inst-shape completeness so task-39 (interp) + task-40 (recompiler) can build on it. Scope = the WHOLE crates/gcn crate (one owner, no internal merge conflict). ITEMS: #1 HIGH (decoder.rs:295) MUBUF soffset decoded via decode_src (not literal-aware): field 255 yields Literal(0) and size_dwords stays 2, dropping the trailing dword, so every following instruction mis-decodes at a shifted offset (PC-desync cascade). Verify whether GFX7/bonaire MUBUF permits a literal soffset; if inline/SGPR-only, do NOT silently emit Literal(0) (route to Unknown or reject); if literal is permitted, read the 3rd dword and advance to 3. #2 (decoder.rs:213,247) Inst DROPS interpreter-critical fields: v_madmk/v_madak K constant (2nd dword) and VOP3 omod bits[28:27] advance the PC correctly but are never stored in Inst::Vop2/Vop3 (no field), so task-39 would use garbage. Add the fields (K to a madmk/madak-carrying shape, omod to Vop3) and print them in disasm. #4 (inst.rs) Inst::Unknown + Decoded lack stream position: add offset_dwords to Decoded and capture any consumed 2nd raw dword in Unknown so task-39/40 can correlate/patch. #3 MED (opcodes.rs) unexercised opcode consts are from-memory: VERIFY all unexercised VOP1/VOP2/VOP3 opcode NUMBERS + names against llvm-mc for -mcpu=bonaire (GFX7/CI) by assembling each mnemonic and disassembling to confirm the op field. NOTE a review finder claimed V_MAD_F32 equals 0x1C1 but that is VI/GFX8 numbering; bonaire=GFX7 where 0x141 is likely correct — llvm-mc is the authority, do NOT blind-apply the finder numbers. Fix any genuinely wrong const (check V_SQRT_F32 0x2E vs 0x2F). #5 (tests/decode.rs + corpus .dis) goldens are regenerated by the decoder-under-test (self-referential): strengthen by adding an llvm-mc disassemble cross-check in the test, or document in decode.rs that goldens are hand-verified against the .s (not regen-blessed) and note it in regen.sh. #8-exp (disasm.rs) exp modifier order: emit the done flag before compr to match llvm-mc/AMD convention (unexercised). #6 (decoder.rs) trace_enabled calls std::env::var per-instruction in the decode_all hot loop — hoist to a OnceLock bool like pm4 TRACE_ENV. #9-cleanup collapse sreg_span/vreg_span into one reg_span; avoid per-instruction String alloc where disasm is not needed; strip the task-37 ref from regen.sh line 19 (conventions.md:20). Keep ps4-gcn Vulkan-free, ps4-core-only plus tracing. No task-NN refs in new comments.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 MUBUF with a literal soffset (if GFX7 permits) advances 3 dwords and reads the literal; if not permitted, field 255 does not silently produce Literal(0); a following instruction decodes at the right offset either way (regression test)
- [ ] #2 Inst carries the madmk/madak K constant and VOP3 omod; both are printed in disasm and available to a consumer; unit tests decode and assert them
- [ ] #3 Decoded has offset_dwords and Inst::Unknown captures the raw consumed word(s); a stream with an unknown multi-dword op reports its position
- [ ] #4 every unexercised VOP1/VOP2/VOP3 opcode const is verified against llvm-mc -mcpu=bonaire; wrong consts fixed; the verification method is documented
- [ ] #5 goldens cross-checked against llvm-mc OR documented as hand-verified-vs-.s; env::var hoisted out of the decode loop; build/test/clippy/fmt green; ps4-gcn Vulkan-free
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
DONE 2026-07-12 (fix/task-87 @<prior-history>, merged). llvm-mc 22.1.6 AVAILABLE — used as authority. #1 MUBUF soffset: llvm-mc CONFIRMS literal NOT permitted on GFX7/bonaire (soffset = SGPR/inline/m0 only); field==255 now emits Operand::Raw(255), stays size_dwords=2 (no phantom dropped dword); test mubuf_invalid_literal_soffset_does_not_drop_dword. #2/#3 Inst additions: Vop2 k: Option<u32> (madmk/madak K, 2nd dword), Vop3 omod: u8 — CORRECTION: omod is bits[28:27] of HIGH dword w1 (review hint said w0; llvm-mc mul:2/mul:4/div:2 confirmed w1); Decoded gained offset_dwords, Inst::Unknown gained raw_words: Vec<u32>. Disasm prints K (madmk: K between srcs, madak: K last) + omod (mul:2/mul:4/div:2). #4 opcode verify vs llvm-mc -mcpu=bonaire: ONLY ONE wrong — V_SQRT_F32 0x2E→0x33 (both code AND review hint 0x2E/0x2F wrong; llvm-mc=0x33). CONFIRMED correct: all VOP1 (V_RCP=0x2A,V_MOV=0x01,CVT), VOP2 (V_MADMK=0x20,V_MADAK=0x21), VOP3 — V_MAD_F32=0x141 (GFX7; finder's 0x1C1=VI/GFX8 correctly REJECTED), V_FMA=0x14B, V_MED3=0x157. Method documented in opcodes.rs header. #5 golden oracle: corpus_bytes_match_llvm_mc re-assembles .s → asserts ==.code.bin (skips if llvm-mc absent, passed); goldens documented hand-verified-vs-.s in decode.rs+regen.sh. exp order done-before-compr; env hoisted to OnceLock<bool> read once before decode_all loop; sreg/vreg_span→reg_span; task-37 ref stripped from regen.sh. FOR TASK-39: omod 1=×2/2=×4/3=÷2 on result; MUBUF soffset Raw(255)=invalid/unsupported; Unknown.raw_words additive (empty in practice — no path emits multi-dword Unknown yet). 23 tests, Vulkan-free. Combined gate: 30 suites, oracle 6/6, exercised goldens unchanged.
<!-- SECTION:NOTES:END -->
